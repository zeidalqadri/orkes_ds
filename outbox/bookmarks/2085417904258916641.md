# Bookmark Triage — 2085417904258916641

Source: @alextalksai, 2026-08-06

## Verdict

**MEDIUM — evaluate once, do not fleet-install.**

The post accurately describes `claude-code-setup`: Anthropic lists it in the official `claude-plugins-official` marketplace as a codebase analyzer that recommends hooks, skills, MCP servers, and subagents. Its value is an external, structured second opinion on Claude Code automation coverage; it is not an autonomous maintenance system or a direct fix for scraper, pricing, or PM2 failures.

## Relevance

| Surface | Relevance | Assessment |
|---|---:|---|
| arbos- | Medium | Useful for a one-time audit of the Claude Code-backed bots' prompt, skill, hook, and expert-fleet boundaries. It does not apply to the `orkes_ds2` OpenCode exception and should not alter its loop contract. |
| sec- | Medium–High | Stronger fit because `orkes_sec` combines portal automation, browser/MCP use, coding workflows, and recurring agent maintenance. The recommendations can expose missing capability guards or duplicated workflow configuration. |

## Why

- The official marketplace entry and plugin reference confirm the claimed scope; the post is not a fabricated plugin promotion.
- ArbOS already has state control, plan-before-write, an expert fleet, shared learnings, and documented browser/MCP patterns. The useful output is a gap comparison, not a replacement architecture.
- PM2 agents run non-interactively and some routes use OpenCode. Installing Claude Code hooks or MCPs globally without compatibility and credential review could add startup latency, unexpected prompts, or unavailable tooling.

## Adopt / Adapt

1. **Run one read-only assessment per repository** — invoke `claude-code-setup` manually in `/home/the_bomb/orkes` and `/home/the_bomb/orkes_sec`; save only a dated recommendation report. Do not accept installations or edits during the scan.
2. **Apply a compatibility gate** — classify each recommendation as `Claude-only`, `OpenCode-compatible`, or `not applicable`; reject anything that modifies global hooks, starts a credentialed MCP, or changes PM2-loop behavior without an approved, tested implementation plan.
3. **Turn accepted findings into bounded work** — map a recommendation to one owner, one repository, expected gain, rollback, and verification. Reuse the existing STATE/GOAL and expert-fleet workflow rather than adding a parallel automation controller.
4. **Prefer existing local capabilities** — prioritize audits of installed skills, existing Browser MCP use, and current task guardrails before adding another MCP server or subagent framework.
5. **Re-run only after material architecture changes** — use it for baseline and post-reconfiguration reviews, not every agent step; recurring scans would consume context without maintaining runtime health.

## Evidence

- Anthropic official marketplace: `claude-code-setup` is authored by Anthropic and described as an analyzer/recommender for hooks, skills, MCP servers, and subagents.
- The plugin's own reference lists standardized workflow and multi-capability setup as appropriate recommendation cases.
